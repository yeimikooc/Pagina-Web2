<!DOCTYPE html>
<html lang="es">
<head>
<meta charset=" sütterlin-8">

<title> ୨♡୧Calculadora Científica ୨♡୧</title>
<a href="servidor web3.html">regresar</a>

<style>
body {
background-color: #ffe4ec; /* Rosita pastel */

font-family: " sütterlin", cursive;
display: flex;
flex-direction: column;
align-items: center;
height: 100vh;
margin: 0;
padding: 20px;
}

h1 {
font-style: italic;
color: black;
margin-bottom: 10px;
}

.info {
background: #ffc0cb;
padding: 10px 15px;
border-radius: 10px;
margin-bottom: 20px;
max-width: 600px;
text-align: justify;
font-size: 14px;
color: #333;
}

.calculator {
background: #ffb6c1;
padding: 20px;
border-radius: 15px;
box-shadow: 0px 4px 10px rgba(0,0,0,0.2);
}

.display {
width: 100%;
height: 50px;
font-size: 20px;
font-style: italic;
text-align: right;
margin-bottom: 10px;
border: none;
border-radius: 8px;
padding: 5px;
}

.buttons {
display: grid;
grid-template-columns: repeat(5, 60px);
gap: 8px;
}

button {
height: 50px;
font-size: 18px;
font-style: italic;
border: none;
border-radius: 10px;
background-color: #ffc0cb;
cursor: pointer;
transition: background 0.2s;
}

button:hover {
background-color: #ff91a4;
}

.equal {
grid-column: span 2;
background-color: #ff69b4;
color: white;
}
</style>
</head>
<body>
<h1>Calculadora Científica</h1>



<div class="calculator">
<input type="text" id="display" class="display" disabled>
<div class="buttons">
<!
-Números -->
<button onclick="append('7')">7</button>
<button onclick="append('8')">8</button>
<button onclick="append('9')">9</button>
<button onclick="append('/')">÷</button>
<button onclick="appendFunction('sqrt')">√</button>
<button onclick="append('4')">4</button>
<button onclick="append('5')">5</button>
<button onclick="append('6')">6</button>
<button onclick="append('*')">×</button>
<button onclick="append('**')">^</button>

<button onclick="append('1')">1</button>
<button onclick="append('2')">2</button>
<button onclick="append('3')">3</button>
<button onclick="append('-')">−</button>
<button onclick="appendFunction('log')">log</button>

<button onclick="append('0')">0</button>
<button onclick="append('.')">.</button>
<button onclick="append('+')">+</button>
<button onclick="clearDisplay()">C</button>
<button class="equal" onclick="calculate()">=</button>

<!
-Funciones científicas -->
<button onclick="appendFunction('sin')">sin</button>
<button onclick="appendFunction('cos')">cos</button>
<button onclick="appendFunction('tan')">tan</button>
<button onclick="appendFunction('ln')">ln</button>
<button onclick="append(')')">)</button>
<!
-Constantes -->
<button onclick="append('Math.PI')">π</button>
<button onclick="append('Math.E')">e</button>
<button onclick="backspace()">⌫</button>
<button onclick="append('(')">(</button>
<button onclick="append(')')">)</button>
</div>
</div>
<script>
const display = document.getElementById('display');

function append(value) {
display.value += value;
}

function appendFunction(func) {
if (func === "sin") display.value += "sin(";
if (func === "cos") display.value += "cos(";
if (func === "tan") display.value += "tan(";
if (func === "sqrt") display.value += "sqrt(";
if (func === "log") display.value += "log(";
if (func === "ln") display.value += "ln(";
}

function clearDisplay() {
display.value = '';
}

function backspace() {
display.value = display.value.slice(0, -1);
}

function calculate() {
try {
let expr = display.value;

// Trigonometría en grados
expr = expr.replace(/sin\(([^)]+)\)/g, (_, angle) =>
`Math.sin((${angle})*Math.PI/180)`);
expr = expr.replace(/cos\(([^)]+)\)/g, (_, angle) =>
`Math.cos((${angle})*Math.PI/180)`);
expr = expr.replace(/tan\(([^)]+)\)/g, (_, angle) =>
`Math.tan((${angle})*Math.PI/180)`);

// Raíz y logaritmos
expr = expr.replace(/sqrt\(([^)]+)\)/g, 'Math.sqrt($1)');
expr = expr.replace(/log\(([^)]+)\)/g, 'Math.log10($1)');
expr = expr.replace(/ln\(([^)]+)\)/g, 'Math.log($1)');

display.value = eval(expr);
} catch {
display.value = 'Error';
}
}
</script>
</body>
</html>
